# Variables de Entorno - Openner Online

# Puerto del servidor (Render lo asigna automáticamente)
PORT=3000

# JWT Secret (genera uno seguro en producción)
# Puedes generar uno con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=tu_super_secreto_jwt_aqui_cambiar_en_produccion

# URL del Frontend (para CORS)
FRONTEND_URL=http://localhost:3000

# Entorno
NODE_ENV=production
